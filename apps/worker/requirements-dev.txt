# Test/dev dependencies for the worker's hermetic suite. The suite is fully
# dependency-injected and imports the heavy runtime deps (anthropic / pypdf /
# apscheduler) only lazily, so it needs just the test tooling + the two libs
# imported at module load (requests, PyYAML). To actually RUN the worker, install
# requirements.txt. Install with:  pip install -r requirements-dev.txt
requests>=2.31
PyYAML>=6.0
pytest>=8
pytest-cov>=5
